import React from "react";

interface GamesProps {
  games: any[];
  error: string;
}
const Games = ({ games, error }: GamesProps) => {
  return <div>Пока что пусто тут</div>;
};

export default Games;
